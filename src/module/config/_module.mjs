import skills from "./skill-config.mjs";
import stats from "./stats-config.mjs";
import characteristics from "./chara-config.mjs"

const config = {
  stats: stats,
  skills: skills,
  characteristics: characteristics
}

export default config;