/**
 * @typedef SystemDataModelMetadata
 */

/**
 * @typedef ItemDataModelMetadata
 */

/**
 * @typedef ActorDataModel
 * @property {boolean} scarrable  Wether or not the actor uses the scar system, taking maluses instead of dying
 */